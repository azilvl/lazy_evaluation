name: lazy_evaluation
description: A simple package to cache and lazily evaluate objects that have a
  expensive initialization process.
version: 0.0.6+3
homepage: https://github.com/azilvl/lazy_evaluation

environment:
  sdk: '>=2.10.0-0 <3.0.0'

dev_dependencies:
  pedantic: ^1.10.0-nullsafety
  test: ^1.16.0-nullsafety.2
